// HScript stuff
// For a main menu hscript file make sure you name it "mainmenu.hx"!

function onCreate()
{
	// triggered when the hscript file is started, some variables weren't created yet
}

function onCreatePost()
{
	// end of "create"
}

function onDestroy()
{
	// triggered when the haxe file is ended (Song fade out finished)
}

function onUpdate(elapsed:Float)
{
	// start of "update", some variables weren't updated yet
}

function onUpdatePost(elapsed:Float)
{
	// end of "update"
}

function onSelectionChange(curSelected:Int)
{
    // triggered when you switch between menu items
}

function onItemSelected(curSelected:Int)
{
    // tiggered when you select an option by pressing Enter
}